#include "models/iforest/node.hpp"

// Node is a plain aggregate; all methods are defined inline in node.hpp.
// This translation unit satisfies the build system and can be extended
// with non-inline helpers in the future without touching public headers.

namespace iforest {} // namespace iforest
